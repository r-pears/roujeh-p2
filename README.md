# Movie App

<!-- REVIEW: The line " in your project." reads like leftover/orphaned text from a template it doesn't fit the rest of the description. Remove it or finish the sentence. -->
<!-- REVIEW: A README is the first thing a reviewer (or future-you) reads. Consider adding sections for: how to run locally (`npm install` / `npm run dev`), what tech is used (React 19 + Vite), and a short feature list (add / edit / delete / persisted in localStorage / seeded from API on first load). It only takes a few minutes and makes the project look much more polished. -->

Simple React app to **add, edit, and delete movies**.  
 in your project.
